name: reorder_grid
description: >
  Layout denso e reordenável para Flutter — empacotamento estável de tiles de
  tamanhos variados, com preview ao vivo durante o arraste.
version: 0.4.0
repository: https://github.com/mozzerai/reorder_grid
issue_tracker: https://github.com/mozzerai/reorder_grid/issues

environment:
  sdk: ">=3.12.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  material_ui: ^1.0.0

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
